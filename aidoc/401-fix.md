You are working on a React + Vite project called Serious Punch Coding (owner app).

The project is at /Users/aj/jsd12/sp-spin3-frontend/owner-app

## The bug

Menu create, delete, and toggle all return 401 Unauthorized.

Root cause: the api.js utility is reading the token from a cookie via getCookie() but the token is saved in localStorage after login. So the Authorization header goes out empty and the backend rejects the request.

## Fix 1 — owner-app/src/utils/api.js

Read the current file first. Then replace the entire file contents with this exact code:

const BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000'

const getHeaders = () => ({
  'Content-Type': 'application/json',
  ...(localStorage.getItem('token') && {
    Authorization: `Bearer ${localStorage.getItem('token')}`,
  }),
})

export const api = {
  get: (path) =>
    fetch(BASE_URL + path, { headers: getHeaders() })
      .then(r => { if (!r.ok) throw new Error(r.statusText); return r.json() }),

  post: (path, body) =>
    fetch(BASE_URL + path, {
      method: 'POST',
      headers: getHeaders(),
      body: JSON.stringify(body),
    }).then(r => { if (!r.ok) throw new Error(r.statusText); return r.json() }),

  patch: (path, body) =>
    fetch(BASE_URL + path, {
      method: 'PATCH',
      headers: getHeaders(),
      body: JSON.stringify(body),
    }).then(r => { if (!r.ok) throw new Error(r.statusText); return r.json() }),

  delete: (path) =>
    fetch(BASE_URL + path, {
      method: 'DELETE',
      headers: getHeaders(),
    }).then(r => {
      if (!r.ok && r.status !== 204) throw new Error(r.statusText)
      return r.status === 204 ? null : r.json()
    }),
}

## Fix 2 — owner-app/src/context/AuthContext.jsx

Read the current file first. Find the login() function.

Check how the token is being saved after a successful login response.

If it is saving to a cookie only — change it to save to localStorage like this:

localStorage.setItem('token', data.token)

If it is already saving to localStorage — leave it as is, do not change anything else in the file.

Also check the logout() function. Make sure it clears localStorage like this:

localStorage.removeItem('token')

If it is only clearing a cookie — add the localStorage line too.

## Rules

- Do not change anything else in any file
- Do not add new dependencies
- Do not rewrite components or pages
- Only touch the two files listed above
- After making changes, show me a summary of exactly what you changed and in which file